# Grant Positioning

Solana Agent is an open-source mission runtime for Solana and Anchor builders.

It turns the local development lifecycle into safe, reproducible, evidence-backed executions:

- scaffold
- test
- deploy
- invoke
- verify
- document

The initial proof of work is a complete `create-counter` mission on devnet.
